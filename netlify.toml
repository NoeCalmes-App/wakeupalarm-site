# Configuration Netlify pour React + Vite
[build]
  command = "npm run build"
  publish = "dist"

# Serve static privacy page directly (for bots/crawlers & Google Play verification)
[[redirects]]
  from = "/privacy"
  to = "/privacy.html"
  status = 200

# Redirection pour React Router (SPA)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Headers de sécurité
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "no-referrer"

# Cache pour les assets
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
